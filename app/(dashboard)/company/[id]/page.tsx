import React from 'react'
import { prepareData } from '@/app/data';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { DataCompany } from '@/app/hooks/useCompanyStore';

export default async function CompanyPage ({params}, children: React.ReactNode): Promise<JSX.Element> {

   

  return (
    <section className="px-4 py-8">
  
  </section>
  )
}
